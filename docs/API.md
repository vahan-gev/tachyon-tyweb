# tyweb — API

Generated by `tachyon doc`.

## module `router`

### Request

- `method: string`
- `path: string`
- `body: string`
- `params: HashMap`

### Response

- `status: int`
- `contentType: string`
- `body: string`

### Route

- `method: string`
- `pattern: string`
- `handler: =>`

### Router

- `routes: Vec`
- `route(...)`
- `get(...)`
- `post(...)`
- `put(...)`
- `delete(...)`
- `dispatch(...)`
- `len(...)`

### `function reqParam(req: &, name: string): string`

A captured `:name` segment ("" when absent).


### `function stripQuery(path: string): string`

Drop `?query=...` from a path.


### `function matchPattern(pattern: string, path: string, out: &mut): bool`

Match `/users/:id` against `/users/7`, filling captures. `*` matches the rest.


### `function splitPath(p: string): Vec`

Split a path into non-empty segments.


### `function serve(app: Router, port: int, workers: int): void`

Run the router: `workers` accept-loops on one socket, panics isolated as 500s.

